# Supabase project config. Set project_id to your project ref when you link with
#   supabase link --project-ref <your-ref>
project_id = "dia"

[functions.dia-count]
# verify_jwt = true means the platform rejects calls without a valid signed-in
# user token. This is what stops strangers from spending your Gemini quota.
verify_jwt = true
